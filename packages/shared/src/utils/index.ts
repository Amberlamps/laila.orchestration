/**
 * @module @laila/shared/utils
 *
 * Pure utility functions used across the monorepo, including pagination
 * helpers, error formatting, ID generation, and other domain-agnostic
 * operations.
 *
 * All functions in this module must be pure (no side effects) and free
 * of Node.js-specific APIs so they can be used in both server and
 * browser contexts.
 */

export {};
